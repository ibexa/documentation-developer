{% extends 'base.html.twig' %}

{% block content %}
    <div class="main-page">
        <h1>PHP API Reference</h1>

        <p>Ibexa DXP's Public PHP API is defined in the <code>Ibexa\Contracts</code> namespace where consumption backward compatibility is promised.</p>

        <p>If those <code>Contracts</code> are used for type hinting (including auto-wiring), the received objects will always respect the <code>Contracts</code> in the next minor versions, following <a href="https://semver.org/">SemVer</a>.</p>
    </div>
{% endblock %}