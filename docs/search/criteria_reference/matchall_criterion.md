---
description: MatchAll Search Criterion
---

# MatchAll Criterion

The `MatchAll` content and `MatchAll` product search criteria are auxiliary criteria that returns all search results.
They're used internally when no filter or query is provided on a Query object.

The criteria take no arguments.
